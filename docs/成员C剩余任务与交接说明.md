# 成员 C 剩余任务与交接说明

---

## 一、成员 C 剩余任务

以下任务需人工完成，无法由程序代劳：

| 任务 | 说明 |
|------|------|
| 收集真实图片 | 前往故宫博物院、国家博物馆等公开渠道，下载 8 类纹样各 2–3 张图片，按命名规范放入 `data/images/各纹样文件夹/`，并在 `docs/图片来源说明.md` 填写来源和授权状态 |
| 制作 PPT | 按 `docs/成员C工作计划.md` 页面分工，制作不少于 15 页的 PPT，嵌入系统真实截图（需等成员 A 提供截图素材）|
| 填写分工表 | 新建 `docs/团队分工表.xlsx`，填写组长、全体组员的学号、姓名、专业、分工 |
| 视频剪辑导出 | 按 `docs/演示视频脚本.md` 剪辑，整合成员 A 前端录屏与架构图，添加字幕和配音，导出 `video/演示视频.mp4` |
| 最终材料检查 | 对照 `docs/提交材料检查清单.md` 逐项核对，确认全部 ✅ 后打包压缩包 |

---

## 二、C 需要从 A 获取的内容

| 需要的内容 | 用途 | 建议时间 |
|------------|------|----------|
| 系统各页面截图（首页、识别页、结果页、知识库页）| 制作 PPT 第 11–14 页的页面展示 | A 完成前端页面后尽快提供 |
| 前端操作录屏（完整流程）| 视频剪辑素材 | 系统联调完成后提供 |

---

## 三、C 需要从 B 获取的内容

| 需要的内容 | 用途 | 建议时间 |
|------------|------|----------|
| 技术架构图 | PPT 第 6 页系统架构页 | B 完成后端后提供 |
| 接口说明（路径、方法、返回字段）| PPT 第 8 页识别逻辑页 | 与架构图同步提供 |
| 后端运行录屏（启动 + 接口返回）| 视频剪辑中技术架构段落的素材 | 系统联调完成后提供 |

---

## 四、C 已完成内容（供 A/B 使用）

| 文件 | 说明 | A/B 使用方式 |
|------|------|-------------|
| `data/patterns.json` | 8 类纹样完整知识库 | B 后端直接读取；A 前端渲染知识库页和结果页 |
| `data/artifacts.json` | 24 条玉器案例 | B 后端读取用于相似案例推荐接口 |
| `data/demo_images.json` | 演示图片与纹样映射 | B 后端识别逻辑核心依据 |
| `docs/AI讲解文本模板.md` | 讲解文本生成模板 | B 后端 `generator.py` 按模板填充字段生成讲解文本 |
| `data/images/（待图片入库）` | 纹样示例图片 | B 后端相似度匹配的样本库；A 前端识别页示例图片展示 |

---

## 五、关键依赖提醒

- **C → B（需尽快）：** 图片收集完成后通知 B，B 才能完善识别逻辑中的样本库匹配
- **A → C（PPT/视频前）：** C 制作 PPT 和剪辑视频前需要 A 提供页面截图和录屏
- **B → C（PPT/视频前）：** C 制作 PPT 前需要 B 提供架构图和接口说明
